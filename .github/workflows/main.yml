name: Main action

on: [push]

jobs:
  checking:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install -r requirements.lock
          python -m pip install -r requirements-dev.lock

      - name: Run flake8
        run: python -m flake8 --max-line-length 88 --format=default ./src/organization_parser 2>&1 | tee flake8.txt

      - name: Run black
        run: python -m black ./src/organization_parser --check

      - name: Run bandit
        run: python -m bandit -ll -iii ./src -f json -o ./bandit.json

      - name: Run mypy
        run: python -m mypy ./src/organization_parser 